# 修复方案（已按用户反馈调整）：wallet_earn 入口接入 + balance_after 修复

## Part 1 — SQL Migration：修复 balance_after + 清理函数

**新建 `migrations/20260814_009_wallet_fix.sql`**

### 1.1 删除旧版 rpc_wallet_earn（消除重载）
只删 8 参数版（oid=31454），保留 9 参数版（oid=31563，含 `p_bypass_cap`）。

### 1.2 修复 rpc_cat_shop_buy 的 balance_after
- `UPDATE public.wallet SET balance = balance - v_total_price` 后加 `RETURNING balance INTO v_new_balance`
- INSERT wallet_log 补 `balance_after` 列，值为 `v_new_balance`

### 1.3 删除 rpc_cat_auto_wage + 同步清理 heartbeat.py 调用点
- **先排查 heartbeat.py 中所有 `auto_wage` / `cat_auto` 调用点，删除或注释掉**
- 再 `DROP FUNCTION IF EXISTS public.rpc_cat_auto_wage(text, int, int)`
- 防止宠物心跳第二天凌晨崩

## Part 2 — home_system.py：零花钱配置 + 便捷函数

### 2.1 读取 WALLET_ALLOWANCE_WEEKLY 环境变量（默认 25）

### 2.2 加 wallet_allowance() 函数
- ISO 周标签格式统一为 `YYYYW##`（如 `2026W33`），与 console.html 对齐
- `source_key = "allowance_2026W33"`，幂等防重
- **标注注释：目前无调用方，为未来扩展（如定时自动发放）备用**

## Part 3 — console.html：钱包操作入口

在钱包面板新增：
- "发零花钱"按钮：调用 `rpc_wallet_earn`（bypass_cap=true, source_key=`allowance_YYYYW##`）
- "打赏"入口：金额输入 + 调用 `rpc_wallet_earn`（bypass_cap=true, source_key=`tip_<时间戳>`）
- ISO 周标签 JS 计算格式与 home_system.py 对齐：`YYYYW##`

## Part 4 — tool_loop.py：更新 wallet_earn 工具描述
让 Finn 知道：做完自主任务可领报酬（5-15元），bypass_cap=false 计周上限，bypass_cap=true 是零花钱/打赏。

## Part 5 — 文档更新
- README.md：删除 auto_wage 引用，更新三条收入来源
- VARIABLES.md：确认 WALLET_ALLOWANCE_WEEKLY 已接入代码

## 不改的部分
钱包框架、周上限80、加班银行、支出/兑换/流水 — 全保留