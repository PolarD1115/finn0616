import sys, traceback

try:
    import tool_loop
    print("tool_loop imported OK")
    print(f"_finalize_weather_activity exists: {hasattr(tool_loop, '_finalize_weather_activity')}")
except Exception as e:
    print(f"Import error: {e}")
    traceback.print_exc()
