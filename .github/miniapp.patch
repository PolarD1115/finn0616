--- console.html	2026-08-15 11:54:33.000000000 +0000
+++ miniapp.html	2026-08-16 08:05:38.554932766 +0000
@@ -2,8 +2,11 @@
 <html lang="zh-CN">
 <head>
 <meta charset="UTF-8"/>
-<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
-<title>网关控制台 · MCP Gateway</title>
+<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, viewport-fit=cover"/>
+<meta name="theme-color" content="#fbf6ec"/>
+<meta name="apple-mobile-web-app-capable" content="yes"/>
+<meta name="apple-mobile-web-app-status-bar-style" content="default"/>
+<title>网关 Mini App · MCP Gateway</title>
 <link rel="preconnect" href="https://fonts.googleapis.com"/>
 <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin/>
 <link href="https://fonts.googleapis.com/css2?family=Noto+Serif+SC:wght@400;500;600;700&family=Playfair+Display:wght@500;600;700&display=swap" rel="stylesheet"/>
@@ -249,6 +252,68 @@
 .pet-info .row{display:flex;justify-content:space-between;border-bottom:1px solid var(--line-soft);padding:3px 0;}
 .pet-info .k{color:var(--sub);}
 .pet-info .v{color:var(--txt);font-weight:500;}
+
+/* ===== Mini App 移动端壳层 ===== */
+html{background:var(--bg);}
+body{padding-bottom:calc(68px + env(safe-area-inset-bottom));}
+.app{display:block;min-height:100%;}
+.sidebar{left:0;right:0;top:auto;bottom:0;width:auto;height:calc(62px + env(safe-area-inset-bottom));transform:none!important;
+  padding-bottom:env(safe-area-inset-bottom);border-right:0;border-top:1px solid var(--line);
+  background:rgba(251,246,236,.97);backdrop-filter:blur(14px);box-shadow:0 -4px 16px rgba(59,50,39,.08);}
+.sidebar .brand,.sidebar .foot{display:none;}
+.nav{display:flex;align-items:stretch;gap:2px;padding:4px 6px;overflow-x:auto;overflow-y:hidden;scrollbar-width:none;}
+.nav::-webkit-scrollbar{display:none;}
+.nav a{flex:0 0 66px;min-height:52px;margin:0;padding:5px 2px;border:0;border-radius:6px;
+  flex-direction:column;justify-content:center;gap:2px;text-align:center;font-size:10px;line-height:1.2;white-space:nowrap;}
+.nav a svg{width:19px;height:19px;}
+.nav a.active{border:0;background:var(--orange-soft);}
+.main{margin-left:0;min-height:100vh;}
+.topbar{padding:calc(8px + env(safe-area-inset-top)) 12px 8px;gap:8px;align-items:center;}
+.topbar .page-ttl{font-size:16px;white-space:nowrap;}
+.topbar .badge{padding:4px 8px;white-space:nowrap;}
+.menu-btn{display:inline-flex;background:var(--paper2);color:var(--txt);border:1px solid var(--line);box-shadow:none;padding:9px;}
+.topbar .secret-wrap{display:none;width:100%;order:10;gap:7px;}
+.topbar.config-open .secret-wrap{display:flex;}
+.topbar.config-open #sbConfigWrap[style]{display:none!important;}
+.topbar.config-open #sbConfigWrap.mobile-visible[style]{display:flex!important;}
+.topbar .secret-wrap input,.topbar .secret-wrap input[style]{width:auto!important;min-width:0;flex:1;padding:10px 11px;}
+.topbar .secret-wrap button{min-width:42px;min-height:42px;justify-content:center;}
+.content{padding:14px 12px 24px;max-width:820px;margin:0 auto;}
+.card{padding:14px 13px;margin-bottom:12px;}
+.card h3{font-size:14px;line-height:1.35;flex-wrap:wrap;}
+.card h3 .sub{max-width:100%;overflow-wrap:anywhere;}
+.grid,.g2,.g3,.g4{grid-template-columns:1fr;gap:10px;}
+#ovStats,.dim-grid,#walletStats,#petInventory,#houseRooms,#houseObjects{grid-template-columns:repeat(2,minmax(0,1fr));}
+button{min-height:42px;padding:9px 13px;justify-content:center;}
+button.icon{width:42px;padding:9px;}
+input,textarea,select{font-size:16px;min-height:44px;}
+.dim-row{align-items:stretch;flex-wrap:wrap;}
+.dim-row>select[style]{width:100%!important;flex:1 1 100%!important;}
+.dim-row>.name{align-self:center;}
+.tbl-wrap{margin-left:-13px;margin-right:-13px;border-left:0;border-right:0;border-radius:0;}
+table{min-width:760px;}
+th,td{padding:10px 9px;}
+.tabs{gap:2px;}
+.tab{padding:10px 12px;}
+.modal-mask{align-items:flex-end;padding:0;}
+.modal{max-width:none;width:100%;max-height:90dvh;border-radius:8px 8px 0 0;padding:18px 15px calc(18px + env(safe-area-inset-bottom));}
+.toast{left:12px;right:12px;bottom:calc(74px + env(safe-area-inset-bottom));max-width:none;text-align:center;}
+.donut-svg{width:160px;height:160px;}
+.room-card,.inv-item{min-width:0;}
+.room-card .desc,.inv-item .name{overflow-wrap:anywhere;}
+
+@media(min-width:760px){
+  .content{padding:20px 20px 32px;}
+  .grid.g2,.grid.g3,.grid.g4{grid-template-columns:repeat(2,minmax(0,1fr));}
+  #ovStats{grid-template-columns:repeat(4,minmax(0,1fr));}
+  .topbar{padding-left:max(20px,calc((100vw - 820px)/2));padding-right:max(20px,calc((100vw - 820px)/2));}
+}
+@media(max-width:390px){
+  .content{padding-left:9px;padding-right:9px;}
+  .card{padding-left:11px;padding-right:11px;}
+  .page-ttl{max-width:120px;overflow:hidden;text-overflow:ellipsis;}
+  #ovStats,.dim-grid,#walletStats,#petInventory,#houseRooms,#houseObjects{gap:8px;}
+}
 </style>
 </head>
 <body>
@@ -258,8 +323,8 @@
     <div class="brand">
       <span class="mark">⌘</span>
       <div>
-        <div class="ttl">网关控制台</div>
-        <div class="sub">MCP Gateway Admin</div>
+        <div class="ttl">网关 Mini App</div>
+        <div class="sub">MCP Gateway Mobile</div>
       </div>
     </div>
     <nav class="nav" id="nav">
@@ -272,13 +337,13 @@
       <a data-p="storage"><i data-lucide="save"></i>上下文与存储</a>
       <a data-p="ticks"><i data-lucide="paw-print"></i>状态面板</a>
     </nav>
-    <div class="foot">v5.0 · 桌面控制台</div>
+    <div class="foot">v5.0 · Mini App</div>
   </aside>
 
   <!-- 主体 -->
   <div class="main">
     <div class="topbar">
-      <button class="menu-btn icon" onclick="toggleSidebar()" title="菜单"><i data-lucide="menu"></i></button>
+      <button class="menu-btn icon" onclick="toggleSidebar()" title="连接设置" aria-label="连接设置"><i data-lucide="key-round"></i></button>
       <span class="page-ttl" id="pageTitle">系统概览</span>
       <span class="sp"></span>
       <span class="badge mute" id="netBadge"><i data-lucide="wifi" style="width:13px;height:13px;vertical-align:middle"></i> 连接中</span>
@@ -495,7 +560,7 @@
 
 <script>
 /* ============================================================
- * 网关控制台 —— 浏览器 → gateway 管理 API → Supabase
+ * 网关 Mini App —— 浏览器 → gateway 管理 API → Supabase
  * API_SECRET 存 localStorage，所有 GET 不返回密钥明文。
  * ============================================================ */
 const IS_FILE_PROTOCOL = location.protocol === "file:";
@@ -560,7 +625,11 @@
   document.getElementById("p-"+name).classList.add("active");
   document.querySelectorAll(".nav a").forEach(a=>a.classList.toggle("active",a.dataset.p===name));
   document.getElementById("pageTitle").textContent=PAGE_TITLES[name];
-  if(window.innerWidth<=1024)document.getElementById("sidebar").classList.remove("open");
+  const activeNav=document.querySelector('.nav a[data-p="'+name+'"]');
+  if(activeNav)activeNav.scrollIntoView({behavior:"smooth",block:"nearest",inline:"center"});
+  document.querySelector(".topbar").classList.remove("config-open");
+  const sbWrap=document.getElementById("sbConfigWrap");
+  if(sbWrap)sbWrap.classList.toggle("mobile-visible",name==="ticks");
   const loaders={overview:loadOverview,models:loadModels,channels:loadChannels,emotion:loadEmotion,memories:loadMemories,profile:loadProfile,storage:loadStorage,ticks:loadStatus};
   if(loaders[name])loaders[name]();
   // 自动刷新
@@ -570,7 +639,12 @@
     }
   }
 }
-function toggleSidebar(){document.getElementById("sidebar").classList.toggle("open");}
+function toggleSidebar(){
+  const bar=document.querySelector(".topbar");
+  bar.classList.toggle("config-open");
+  const sbWrap=document.getElementById("sbConfigWrap");
+  if(sbWrap)sbWrap.classList.toggle("mobile-visible",document.getElementById("p-ticks").classList.contains("active"));
+}
 
 /* ---- 启动 ---- */
 window.addEventListener("DOMContentLoaded",()=>{
@@ -579,7 +653,7 @@
   refreshIcons();
   setNet("mute","就绪");
   if(IS_FILE_PROTOCOL){
-    toast("⚠️ 检测到本地文件打开。请通过网关访问：http://localhost:10000/console","err");
+    toast("检测到本地文件打开，请通过网关访问：http://localhost:10000/miniapp","err");
     setNet("no","file:// 模式");
     return;
   }
